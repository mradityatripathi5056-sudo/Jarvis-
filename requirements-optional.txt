# requirements-optional.txt
# ------------------------------------------------------------
# In sabko ek saath install karna ZAROORI NAHI hai. Sirf wahi
# install karo jo skill tumhe chahiye - baaki skills bina inke bhi
# load ho jaayengi, bas unke actions "package missing" bolenge.
#
# Poora sab kuch ek saath try karna ho (dhyan rahe, ismein time
# lagega aur kuch OS-specific issues aa sakte hain):
#   pip install -r requirements-optional.txt
# ------------------------------------------------------------

# Gmail / Google Calendar / Google Drive
google-auth-oauthlib==1.2.1
google-api-python-client==2.140.0
google-auth-httplib2==0.2.0

# Outlook / Microsoft Graph (email + calendar + OneDrive)
msal==1.28.0

# Dropbox
dropbox==12.0.2

# Browser deep automation (form fill, scraping, multi-tab)
playwright==1.46.0
# Install ke baad ye bhi chalao: playwright install chromium

# Network tools
speedtest-cli==2.1.3

# Smart home (Kasa/Tapo direct control; Home Assistant/Hue sirf 'requests' se hi ho jaata hai)
python-kasa==0.7.4

# Vision / OCR
pytesseract==0.3.13
# + Tesseract-OCR binary bhi alag se install karna hoga (README dekho)

# Extra utilities skill - QR code generation
qrcode[pil]==7.4.2

# Math & logic solver (equations, expressions, simplification)
sympy==1.13.2

# Document summarizer (PDF / Word / PowerPoint text extraction)
pypdf==4.3.1
python-docx==1.1.2
python-pptx==1.0.2

# Webcam object detection (YOLOv8n) - pehli baar chalne par ~6MB
# model khud download hoga, uske liye ek baar internet chahiye
ultralytics==8.3.28

# Face Recognition Security + Stealth Mode - opencv-python ke bajaye
# ye "contrib" version use karo (isme cv2.face module hota hai).
# NOTE: dono ek saath install mat karo (conflict hoga) -
#   pip uninstall opencv-python
#   pip install opencv-contrib-python
opencv-contrib-python==4.10.0.84

# Telegram voice note support (bot ko voice message bhejo, text mein convert
# hoke command chalegi) - iske alawa system pe ffmpeg bhi install hona zaroori
# hai (https://ffmpeg.org/download.html - Windows pe zip download karke uska
# bin folder PATH mein daalo)
pydub==0.25.1

# API Integrations
spotipy==2.24.0
PyGithub==2.4.0
boto3==1.35.0
docker==7.1.0
mysql-connector-python==9.0.0
pymongo==4.8.0
